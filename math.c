#include <stdio.h>
int main()
{
    int x = 10;
    int y = 3;
    int r = 0;

    // r = x + y;
    // r = x - y;
    // r = x * y;
    // r = x * y;
    // r = x / y; // ! need to be float
    // r = x % y;
    // x +=2;
    // x -=3;
    // x *=3;
    x /=2;
    printf("%d", x);
}
